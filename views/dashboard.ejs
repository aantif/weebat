<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de Bord</title>
    <link rel="stylesheet" href="/public/css/style.css"> <!-- Lien vers un fichier CSS si existant -->
</head>
<body>

    <header>
        <h1>Bienvenue, <%= user.name %> 👋</h1>
        <nav>
            <a href="/clients">Clients</a>
            <a href="/chantiers">Chantiers</a>
            <a href="/employees">Employés</a>
            <a href="/invoices">Factures</a>
            <a href="/stocks">Stock</a>
            <a href="/logout">Déconnexion</a>
        </nav>
    </header>

    <main>
        <h2>Tableau de Bord</h2>

        <% if (session.message) { %>
            <div class="message <%= session.message.type %>">
                <%= session.message.text %>
            </div>
        <% } %>

        <section class="stats">
            <div class="stat">
                <h3>📋 Total Clients</h3>
                <p><%= totalClients %></p>
            </div>

            <div class="stat">
                <h3>🏗️ Total Chantiers</h3>
                <p><%= totalChantiers %></p>
            </div>

            <div class="stat">
                <h3>👷 Total Employés</h3>
                <p><%= totalEmployees %></p>
            </div>

            <div class="stat">
                <h3>📑 Total Factures</h3>
                <p><%= totalInvoices %></p>
            </div>

            <div class="stat">
                <h3>📦 Stock Disponible</h3>
                <p><%= totalStock %></p>
            </div>
        </section>
    </main>

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        header {
            background: #333;
            color: white;
            padding: 15px;
            text-align: center;
        }

        nav a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
        }

        main {
            width: 80%;
            margin: 20px auto;
            text-align: center;
        }

        .stats {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
        }

        .stat {
            background: white;
            padding: 20px;
            margin: 10px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .message {
            padding: 10px;
            margin: 10px 0;
            text-align: center;
            border-radius: 5px;
        }

        .message.success {
            background-color: #d4edda;
            color: #155724;
        }

        .message.danger {
            background-color: #f8d7da;
            color: #721c24;
        }
    </style>

</body>
</html>

